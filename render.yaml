services:
- type: web
  name: dataset-cleanser-api
  env: python
  plan: starter
  runtime: python-3.11.9
  buildCommand: "pip install -r requirements.txt"
  startCommand: "uvicorn main:app --host 0.0.0.0 --port $PORT"
